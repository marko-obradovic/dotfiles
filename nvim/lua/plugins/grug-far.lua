-- grug-far.nvim is a Neovim plugin that provides an enhanced
-- “find and replace” interface (like :s but smarter and easier to use).
return {
  'MagicDuck/grug-far.nvim',
  config = function()
    -- optional setup
    -- you could override defaults like:
    -- vim.g.grug_far = { ... }
  end,
}

